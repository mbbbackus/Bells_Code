#!/usr/bin/env python
import sys
projPath = sys.path[0]
prebellPath = "bells/prebell.wav"
postbellPath = "bells/postbell.wav"
musicPath = projPath + "/music/"
schedulePath = projPath + "/schedules/" + "default_lol.json"

#schedulePath = projPath + "/schedules/" + "test.json"
